# Nieweb.Web

React 19 + TypeScript + Vite SPA. Scaffolded by Phase 1 backlog item **F1**.

Placeholder for now so the intended project layout is visible in git; the
actual `package.json`, `vite.config.ts`, `src/`, and `index.html` land when
F1 is picked up.

Build output will be copied into `../Nieweb.Api/wwwroot/` on publish.
